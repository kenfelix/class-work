# Boolean is used for categorical output, since the output of boolean is either true or false.

is_available = True

print(type(is_available))

is_greater = 3>4

print(is_greater, is_available, 4>5)

print(int(is_available), float(is_greater))
print(str(is_greater), type(str(is_greater)))

